/** Público-alvo comercial do plano (trial e pagos podem ser BOTH). */
export enum PlanAudience {
  INDIVIDUAL = 'INDIVIDUAL',
  COMPANY = 'COMPANY',
  BOTH = 'BOTH',
}

export enum PlanInterval {
  MONTHLY = 'MONTHLY',
  YEARLY = 'YEARLY',
}
