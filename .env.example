# Copie para .env e preencha. CRYPT_SALT = custo do bcrypt (número de rounds).
# Alias legado: CRYPT_SAULT (ainda lido por src/utils/crypt.ts se CRYPT_SALT estiver vazio).

NODE_ENV=development
APP_PORT=3001

DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=
DB_PASSWORD=
DB_NAME=

JWT_SECRET=
CRYPT_SALT=10
